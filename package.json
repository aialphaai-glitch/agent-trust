{
  "name": "agent-trust-protocol",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*", "examples/*"],
  "scripts": {
    "build": "npm --workspace @atp/sdk run build && npm --workspace @atp/ledger run build && npm --workspace @atp/mcp-server run build && npm --workspace reference-agent run build",
    "test": "npm run test --workspaces --if-present",
    "quickstart": "npm run build && npm --workspace quickstart-ts run start"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.15.0"
  }
}
