# Must live in /etc/nginx/conf.d/ (http context). The site config references
# zone=atp_ledger. 10 requests/second per client IP, 10 MB zone (~160k IPs).
limit_req_zone $binary_remote_addr zone=atp_ledger:10m rate=10r/s;
